# Betreiber-Blick — lokale Vorschau

```
git pull
npm install
npm run dev
```

Dann im Browser: **http://localhost:3000**

(Für einen produktionsnäheren Test statt `npm run dev`: `npm run build && npm start`.)
